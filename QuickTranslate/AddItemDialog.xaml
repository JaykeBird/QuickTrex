<Window x:Class="QuickTranslate.AddItemDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuickTranslate"
        mc:Ignorable="d" ShowInTaskbar="False" Topmost="True"
        Title="Add Item" Height="150" Width="350" MinHeight="150" MaxHeight="155" MinWidth="250" 
        Loaded="Window_Loaded" SourceInitialized="Window_SourceInitialized" WindowStartupLocation="CenterScreen" Icon="Icon.ico" >
    <Grid>
        <TextBlock x:Name="txtDesc" HorizontalAlignment="Left" Margin="20,20,0,0" TextWrapping="Wrap" Text="Name of the item:" VerticalAlignment="Top"/>
        <Button x:Name="btnOK" Content="OK" HorizontalAlignment="Right" Margin="0,0,100,10" Width="75" Height="24" VerticalAlignment="Bottom" Click="btnOK_Click" />
        <Button x:Name="btnCancel" Content="Cancel" HorizontalAlignment="Right" Margin="0,0,20,10" Width="75" Height="24" VerticalAlignment="Bottom" Click="btnCancel_Click" />
        <TextBox x:Name="txtName" Height="23" Margin="20,40,20,0" VerticalAlignment="Top" KeyDown="txtName_KeyDown" />

    </Grid>
</Window>
